---
apiVersion: v1
kind: Service
metadata:
  name: gitlab-https
spec:
  type: ClusterIP #ClusterIP
  selector:
    app: gitlab
  ports:
    - name: "nignx"
      port: 443
    - name: "ssh"
      port: 22
